/**
 * This component provides a note section component, which is a collection of note blocks. Note sections correspond to each of the C-A-P sections.
 */

import React, { useState, useEffect } from 'react';

export default function NoteSection({}): JSX.Element {
  return <> </>;
}
